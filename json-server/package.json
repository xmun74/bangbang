{
  "name": "json-server",
  "version": "1.0.0",
  "description": "테스트서버",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch ./db.json --port 5000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
